import React,{useRef,useState} from 'react';
import {cleanHTML} from './html.js';
import {X,Plus,Save,BookOpen,Film,ClipboardCheck} from 'lucide-react';
import {useApp} from './store.jsx';
import {chapters,topics} from './content.js';
import {periodsOf,safeUrl} from './learning.js';

const escape=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function baseHTML(l){return '<h2>Kiến thức trọng tâm</h2>'+l.concepts.map(c=>'<p>'+escape(c)+'</p>').join('')+'<h2>Quan sát & vận dụng</h2><p>'+escape(l.activity)+'</p>'}
function RichEditor({value,onChange}){const ref=useRef(null),[mode,M]=useState('edit'),[link,L]=useState('');const command=(c,v)=>{ref.current?.focus();document.execCommand(c,false,v);onChange(ref.current.innerHTML)};return <div className="rich-editor"><div className="editor-modes">{[['edit','Soạn thảo'],['preview','Xem trước'],['html','Mã HTML']].map(([k,t])=><button type="button" key={k} className={mode===k?'selected':''} onClick={()=>M(k)}>{t}</button>)}</div>{mode==='edit'?<><div className="format-bar">{[['bold','Đậm'],['italic','Nghiêng'],['underline','Gạch chân'],['insertUnorderedList','• Danh sách'],['insertOrderedList','1. Danh sách'],['undo','Hoàn tác'],['redo','Làm lại'],['removeFormat','Xoá định dạng']].map(([c,t])=><button type="button" key={c} onMouseDown={e=>e.preventDefault()} onClick={()=>command(c)}>{t}</button>)}<button type="button" onClick={()=>command('formatBlock','h2')}>Tiêu đề</button><button type="button" onClick={()=>command('formatBlock','p')}>Văn bản</button><button type="button" onClick={()=>command('insertHTML','<table><tbody><tr><th>Nội dung</th><th>Ghi nhớ</th></tr><tr><td>…</td><td>…</td></tr></tbody></table><p><br></p>')}>Chèn bảng</button><label>Màu chữ<input type="color" onChange={e=>command('foreColor',e.target.value)}/></label></div><div ref={el=>{if(el&&el!==ref.current){el.innerHTML=cleanHTML(value);ref.current=el}}} role="textbox" aria-label="Nội dung định dạng" aria-multiline="true" contentEditable suppressContentEditableWarning className="rich-content" onBlur={e=>onChange(cleanHTML(e.currentTarget.innerHTML))}/><div className="toolbar"><input aria-label="Liên kết chèn vào bài" type="url" placeholder="https://…" value={link} onChange={e=>L(e.target.value)}/><button type="button" onClick={()=>{if(safeUrl(link))command('insertHTML','<p><a href="'+escape(safeUrl(link))+'">'+escape(link)+'</a></p>')}}>Chèn liên kết</button></div></>:mode==='html'?<label>Mã HTML đã lọc khi lưu<textarea value={value} onChange={e=>onChange(e.target.value)} rows={14}/></label>:<div className="rich-content" dangerouslySetInnerHTML={{__html:cleanHTML(value)}}/>}</div>}
const names={none:'Không có media',video:'Video · YouTube / Vimeo / MP4',slides:'Slide · Google Slides / Canva',document:'Tài liệu · PDF / Word / Drive',web:'Trang web / E-learning'};
export default function LessonEditor({entity,close}){const a=useApp();const [l,L]=useState(()=>{const x=entity||{id:crypto.randomUUID(),title:'',chapter:0,topic:'intro',minutes:45,hook:'Cùng tìm hiểu vấn đề khoa học.',concepts:['Kiến thức trọng tâm cần đạt.'],activity:'Vận dụng kiến thức để giải thích hiện tượng.'};return {...x,periods:periodsOf(x).map(p=>({...p,html:p.html||baseHTML(x)}))}}),[selected,S]=useState(0),[error,E]=useState('');const p=l.periods[selected];const set=(key,v)=>L(x=>({...x,[key]:v}));const patch=v=>L(x=>({...x,periods:x.periods.map((p,i)=>i===selected?{...p,...v}:p)}));const quiz=v=>patch({quiz:{...p.quiz,...v}});const save=async ev=>{ev.preventDefault();E('');try{await a.act('saveEntity',{collection:'lessons',entity:{...l,periods:l.periods.map(p=>({...p,html:cleanHTML(p.html)}))}});close()}catch(e){E(e.message)}};return <div className="modal-backdrop"><div className="modal lesson-modal" role="dialog" aria-modal="true" aria-label="Biên soạn bài và tiết học"><button className="modal-close" aria-label="Đóng" onClick={close}><X/></button><header><span className="eyebrow">XƯỞNG HỌC LIỆU KHTN 9</span><h2>Biên soạn bài & tiết học</h2><p>Thiết kế trải nghiệm học từ kiến thức đến vận dụng.</p></header><form onSubmit={save}><label>Tên bài học<input required maxLength={200} value={l.title} onChange={e=>set('title',e.target.value)}/></label><div className="form-grid"><label>Chương<select value={l.chapter??0} onChange={e=>set('chapter',+e.target.value)}>{chapters.map(c=><option key={c.id} value={c.id}>{c.id===0?'Mở đầu':c.id+'. '+c.name}</option>)}</select></label><label>Mạch kiến thức<select value={l.topic} onChange={e=>set('topic',e.target.value)}>{topics.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label><label>Thời lượng bài (phút)<input type="number" min={1} max={180} value={l.minutes} onChange={e=>set('minutes',+e.target.value)}/></label></div><label>Câu hỏi gợi mở<input required maxLength={2000} value={l.hook} onChange={e=>set('hook',e.target.value)}/></label><div className="period-tabs">{l.periods.map((t,i)=><button type="button" key={t.id} className={i===selected?'selected':''} onClick={()=>S(i)}>Tiết {t.order} {!t.unlocked?'· Khoá':''}</button>)}<button type="button" disabled={l.periods.length>=30} onClick={()=>{const n={id:crypto.randomUUID(),title:'Tiết học mới',order:Math.max(...l.periods.map(p=>p.order))+1,minutes:45,unlocked:false,html:'<h2>Yêu cầu cần đạt</h2><p>…</p>',media:{type:'none',url:''},attachments:[],quiz:{enabled:false,required:false,passScore:5,maxAttempts:0,questionIds:[]}};set('periods',[...l.periods,n]);S(l.periods.length)}}><Plus size={15}/>Thêm tiết</button></div><section className="editor-section"><h3><BookOpen size={19}/>Thông tin tiết học</h3><label>Tiêu đề tiết<input required maxLength={200} value={p.title} onChange={e=>patch({title:e.target.value})}/></label><div className="form-grid"><label>Quyền truy cập<select value={String(p.unlocked)} onChange={e=>patch({unlocked:e.target.value==='true'})}><option value="true">Mở cho học sinh</option><option value="false">Khoá · đang biên soạn</option></select></label><label>Thứ tự tiết<input type="number" required min={1} value={p.order} onChange={e=>patch({order:+e.target.value})}/></label><label>Thời lượng (phút)<input type="number" required min={1} max={180} value={p.minutes} onChange={e=>patch({minutes:+e.target.value})}/></label></div></section><section className="editor-section blue"><h3><Film size={19}/>Media chính & tài liệu bổ sung</h3><div className="form-grid"><label>Loại media<select value={p.media.type} onChange={e=>patch({media:{...p.media,type:e.target.value}})}>{Object.entries(names).map(([v,t])=><option key={v} value={v}>{t}</option>)}</select></label>{p.media.type!=='none'&&<label>Liên kết media<input type="url" required placeholder="https://…" value={p.media.url} onChange={e=>patch({media:{...p.media,url:e.target.value}})}/></label>}</div>{p.attachments.map((r,i)=><div className="attachment-edit" key={i}><select aria-label={'Loại tài liệu '+(i+1)} value={r.type} onChange={e=>patch({attachments:p.attachments.map((x,j)=>i===j?{...x,type:e.target.value}:x)})}>{Object.entries(names).filter(([k])=>k!=='none').map(([v,t])=><option key={v} value={v}>{t}</option>)}</select><input aria-label={'Tên tài liệu '+(i+1)} placeholder="Tên tài liệu" required value={r.title} onChange={e=>patch({attachments:p.attachments.map((x,j)=>i===j?{...x,title:e.target.value}:x)})}/><input aria-label={'URL tài liệu '+(i+1)} type="url" required placeholder="https://…" value={r.url} onChange={e=>patch({attachments:p.attachments.map((x,j)=>i===j?{...x,url:e.target.value}:x)})}/><button type="button" aria-label={'Bỏ tài liệu '+(i+1)} onClick={()=>patch({attachments:p.attachments.filter((_,j)=>i!==j)})}><X size={16}/></button></div>)}<button type="button" className="secondary" disabled={p.attachments.length>=10} onClick={()=>patch({attachments:[...p.attachments,{title:'',type:'document',url:''}]})}>+ Thêm tài liệu</button></section><section className="editor-section purple"><h3><ClipboardCheck size={19}/>Củng cố & điều kiện hoàn thành</h3><label className="checkbox"><input type="checkbox" checked={p.quiz.enabled} onChange={e=>quiz({enabled:e.target.checked,required:e.target.checked&&p.quiz.required})}/>Bật trắc nghiệm củng cố</label>{p.quiz.enabled&&<><div className="form-grid"><label>Điểm đạt (thang 10)<input type="number" min={0} max={10} step={0.5} value={p.quiz.passScore} onChange={e=>quiz({passScore:+e.target.value})}/></label><label>Số lượt tối đa (0 = không giới hạn)<input type="number" min={0} max={20} value={p.quiz.maxAttempts} onChange={e=>quiz({maxAttempts:+e.target.value})}/></label></div><label className="checkbox"><input type="checkbox" checked={p.quiz.required} onChange={e=>quiz({required:e.target.checked})}/>Bắt buộc đạt điểm để hoàn thành tiết</label><details><summary>Chọn câu hỏi · Đã chọn {p.quiz.questionIds.length}</summary><div className="question-picker">{a.school.questions.map(q=><label key={q.id} className="checkbox"><input type="checkbox" checked={p.quiz.questionIds.includes(q.id)} onChange={e=>quiz({questionIds:e.target.checked?[...p.quiz.questionIds,q.id]:p.quiz.questionIds.filter(id=>id!==q.id)})}/><span>{q.text}</span></label>)}</div></details></>}</section><section className="editor-section"><h3>Nội dung chi tiết · Tiết {p.order}</h3><div className="template-bar">{[['Yêu cầu cần đạt','Nêu được…; giải thích được…; vận dụng được…'],['Công thức & đơn vị','Đại lượng — Kí hiệu — Đơn vị SI — Điều kiện áp dụng.'],['Thí nghiệm an toàn','Dụng cụ; dự đoán; thao tác; quan sát; kết luận; lưu ý an toàn.'],['Mẹo ghi nhớ','Viết mẹo ngắn gọn, kiểm tra tính chính xác và phạm vi áp dụng.']].map(([t,b])=><button type="button" key={t} onClick={()=>patch({html:p.html+'<h2>'+t+'</h2><p>'+b+'</p>'})}>+ {t}</button>)}</div><RichEditor key={p.id+'-'+(p.html.match(/<h2>/g)||[]).length} value={p.html} onChange={html=>patch({html})}/></section>{error&&<p role="alert" className="error">{error}</p>}<div className="sticky-actions actions"><button type="button" className="secondary" onClick={close}>Huỷ</button><button className="primary" disabled={a.busy}><Save size={17}/>Lưu bài và tiết học</button></div></form></div></div>}
