import React,{useState} from 'react';import {useApp} from './store.jsx';
export default function AISettings(){const a=useApp();const [knowledge,K]=useState(a.school.settings.knowledge||''),[saved,S]=useState(false);return <section className="panel"><h2>Cấu hình trợ lý KHTN 9</h2><p>Supabase: <b>{a.config?.configured?'Đã cấu hình':'Chưa kết nối'}</b> · Gemini: <b>{a.config?.aiConfigured?'Đã cấu hình key hệ thống':'Chưa cấu hình key hệ thống'}</b></p><form onSubmit={async e=>{e.preventDefault();try{await a.act('aiSettings',{models:new FormData(e.target).get('models'),knowledge});S(true)}catch{}}}><label>Danh sách model theo thứ tự ưu tiên<input name="models" defaultValue={a.school.settings.aiModels||'gemini-2.5-flash'} required placeholder="model-1,model-2" maxLength={300}/></label><p className="muted">Chỉ dùng model được tài khoản Gemini hỗ trợ. Chuyển model khi dịch vụ quá tải hoặc lỗi tạm thời.</p><label>Tải kho kiến thức (.txt, .md; tối đa 30 KB)<input type="file" accept=".txt,.md,text/plain,text/markdown" onChange={async e=>{const f=e.target.files?.[0];if(!f)return;if(f.size>30000){a.setError('Kho kiến thức tối đa 30 KB.');return}K(await f.text());S(false)}}/></label><label>Nội dung tham chiếu<textarea rows={9} value={knowledge} onChange={e=>{K(e.target.value);S(false)}}/></label><small>{new TextEncoder().encode(knowledge).length}/30.000 byte. Nội dung được gửi làm tham khảo, không thay thế kiểm chứng của giáo viên.</small><div className="actions"><button className="primary" disabled={a.busy}>Lưu cấu hình AI</button>{saved&&<span role="status">Đã lưu cấu hình.</span>}</div></form><p className="muted">System API key được quản trị viên nhập ở biến môi trường Vercel để giữ bí mật phía máy chủ.</p></section>}
