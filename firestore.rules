rules_version = '2';
service cloud.firestore {
 match /databases/{database}/documents {
  // Direct clients are denied. The authenticated Vercel API handles authorization.
  match /{document=**} { allow read, write: if false; }
 }
}
